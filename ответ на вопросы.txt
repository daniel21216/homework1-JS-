1. Обьяснить своими словами разницу между обьявлением переменных через var, let и const. 
2. Почему объявлять переменную через var считается плохим тоном?
1) Ну Основная разница между var, let и const заключается в их области видимости, возможности повторного объявления и изменения значения
2) Объявлять переменные через var в основном приводят к проблемам с областью видимости